/** Absolute public URL for brand assets (respects Vite `base`). */
export const BRAND_BASE = import.meta.env.BASE_URL;

export const LOGO_SVG = `${BRAND_BASE}logo.svg`;
export const LOGO_PNG = `${BRAND_BASE}logo.png`;
