/**
 * Public single-tip entry (advanced). Prefer TipLayer via Tour for production tours.
 * Kept so `import { Tip } from '@persianstudio/tourara'` stays stable.
 */
export { TipMarker as Tip } from './TipMarker';
